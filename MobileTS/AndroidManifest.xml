<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
	<application android:allowBackup="true" android:icon="@mipmap/appicon" android:label="${appLabel}" android:supportsRtl="true">
		<!-- Отдаём файлы логов/крашрепортов другим приложениям через content:// (системная «шторка»).
		     Authority = "<applicationId>.fileprovider": привязан к пакету, иначе debug и release
		     рядом конфликтуют (INSTALL_FAILED_CONFLICTING_PROVIDER). ${applicationId} подставляет SDK. -->
		<provider
			android:name="androidx.core.content.FileProvider"
			android:authorities="${applicationId}.fileprovider"
			android:exported="false"
			android:grantUriPermissions="true">
			<meta-data
				android:name="android.support.FILE_PROVIDER_PATHS"
				android:resource="@xml/file_paths" />
		</provider>
	</application>
	<uses-permission android:name="android.permission.INTERNET" />
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
	<uses-permission android:name="android.permission.RECORD_AUDIO" />
	<uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
	<uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
</manifest>